// import java.time.LocalDate;
// import java.time.LocalDateTime;
// import java.time.LocalTime;
// import java.time.format.DateTimeFormatter;

public class date_api {
    public static void main(String[] args) {
        /* ---------- You can do uncomment the methord which you want to use. ---------- */

        // LocalDate d = LocalDate.now(); // this is use to print local date.
        // System.out.println(d);

        // LocalTime t = LocalTime.now(); // this is used to print local time.
        // System.out.println(t);

        // LocalDateTime ld = LocalDateTime.now(); // this is used to print date and time.
        // System.out.println(ld);

        // Format date and time
        // LocalDateTime dt = LocalDateTime.now();
        // System.out.println(dt);

        // DateTimeFormatter df = DateTimeFormatter.ofPattern("dd-MM-yy"); // here we give format how we want to print date.
        // DateTimeFormatter df2 = DateTimeFormatter.ISO_LOCAL_DATE; // this is how we have to print date by function.
        // DateTimeFormatter df3 = DateTimeFormatter.ofPattern("dd-MM-yy --E"); // here we give format how we want to print date with name of the day.
//         DateTimeFormatter df3 = DateTimeFormatter.ofPattern("dd-MM-yy a"); // here we give format how we want to print date with am or pm.
//         String mydate = dt.format(df3); // here we mix datetime with formatter
//         System.out.println(mydate);
    }
}
